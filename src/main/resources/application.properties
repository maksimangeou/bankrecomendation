# Primary H2 Database (existing)
spring.datasource.url=jdbc:h2:/Users/maksimzadoroznyj/IdeaProjects/Recomendation/data/transaction
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=
spring.datasource.password=

spring.sql.init.mode=never
spring.h2.console.enabled=true

# Secondary PostgreSQL Database for dynamic rules
app.datasource.secondary.url=jdbc:postgresql://localhost:5432/telegram_bot_db
app.datasource.secondary.username=maksimzadoroznyj
app.datasource.secondary.password=
app.datasource.secondary.driver-class-name=org.postgresql.Driver

# JPA Configuration for secondary database
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Disable Liquibase for primary datasource
spring.liquibase.enabled=true

# Cache configuration
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=1000,expireAfterWrite=10m

# Logging
logging.level.pro.sky.bankrecomendation=DEBUG